import path from 'path';

export const vueFiles = path.resolve(__dirname, '../fixtures/vue-files/**/*.?(js|vue)');
export const languageFiles = path.resolve(__dirname, '../fixtures/language-files/**/*.?(js|json|yml|yaml)');
